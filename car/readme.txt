To run the demo VOC21_ssd_detection_demo.py, you should install caffe-ssd at first. And then change the following directory: caffe_root 
labelmap_file(for labelmap_voc.prototxt)
model_def(for deploy.prototxt)
model_weights(for SSDmodel)
image_dir 
image_list(the format as '000001.jpg')
